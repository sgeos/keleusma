COMPLETION CONDITION — the 2026-09-23 continuation is recorded

Ordering is not a completion criterion. No property below depends on the order
the work was done in, on which branch it happened on, or on any process step.
Any sequence of actions leaving the tree with these properties is complete.

1. The handoff's newest narrative section describes the work done after the
   toolchain block cleared, and no section claiming to be the place to resume
   describes a tree older than another such section in the same file.

2. That description records, as plainly as it records what was achieved, the
   instances in which a record or instrument written in this session claimed more
   than the tree supported, and the count it states matches what the audits
   actually found.

3. The handoff's pickup list reflects what is actually open. If no development
   item is open, it says so rather than listing a closed one as next.

4. Nothing in the handoff describes the toolchain as blocked, or describes as
   unmeasured something the tree now measures.

5. Every figure the handoff states is either re-derivable and matching the tree
   or names the commit it was measured at, and the guards that check it pass.

6. src/ and tests/ at the repository root are unchanged by this session except
   for changes the other line itself made.

7. The backend suite passes in both float configurations, and that rests on a
   whole gate run per configuration rather than on separately read phase
   summaries, on targeted runs selected by binary name, or on one run assumed to
   cover both.

8. The working tree is clean, and the branch on the remote matches the local
   branch, established by querying the remote.
