COMPLETION CONDITION — dynamic arena touch for the streaming corpus modules

Ordering is not a completion criterion. No property below depends on the order
the work was done in, on which branch it happened on, or on any process step.
Any sequence of actions leaving the tree with these properties is complete.

1. Either the tree measures, by running them, how much of the published arena
   figure each streaming corpus module outside the self-hosted stages actually
   touches, or it records why those modules cannot be driven that way, naming
   what was tried.

2. Any such measurement fills the region with more than one pattern, so that a
   byte written with a fill value cannot hide.

3. The tree carries evidence that the measurement can detect a touch, meaning a
   recorded perturbation that moves the reported extent. An argument that it
   would detect one is not evidence.

4. The number of modules driven and the number skipped are both recorded, and a
   run that drove fewer than the stated population fails rather than reporting a
   figure for a narrower one.

5. If any figure quoted in the outgoing report to the other line changes, that
   report is corrected in place, and the guard watching those figures passes.

6. Nothing in the tree describes the arena reservation as a defect, and the
   region planner is unchanged.

7. src/ and tests/ at the repository root are unchanged by this session except
   for changes the other line itself made.

8. The backend suite passes in both float configurations, and that rests on a
   whole gate run per configuration rather than on separately read phase
   summaries, on targeted runs selected by binary name, or on one run assumed to
   cover both.

9. Any harness newly allocating host buffers for a native call derives its
   private region size from the published contract and is declared wherever the
   tree enumerates such harnesses.

10. The handoff state table agrees with the tree, figures that cannot be
    re-derived name the commit they were measured at, and the guards that check
    it pass.

11. The working tree is clean, and the branch on the remote matches the local
    branch, established by querying the remote.
