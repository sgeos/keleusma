# ABSORPTION 71 — the prediction that presumed its own antecedent

**Computed against** `a727c754f1ae205540f63d101a1e551f27b9030d`, with **2 unabsorbed** commits on `origin/v0.2.3`
(`21745dc9`, `4d74909e`). Figures derived at that tree.

## What is arriving

A handoff refresh with a staleness check, and its journal entry:
`docs/process/HANDOFF.md` and `docs/process/DESIGN_JOURNAL.md`. Nothing else.

## Predictions

1. **The merge is clean.** `merge-tree` computes no conflict at the stamp above.

2. ⚠ **THE PREVIOUS ITERATION'S REACH PREDICTION IS NOT CONFIRMED HERE, AND THE FAULT IS
   CALIBRATION RATHER THAN DIRECTION.** It said the next absorption "will most likely flip
   reach through `REVERSE_PROMPT.md` or `docs/decisions`, not `src/`". **This absorption
   touches ZERO reach paths.** The directional half held — `src/` is untouched — but the
   sentence PRESUMED a flip would happen, and none did.

   The base rates measured in the previous increment already implied this. At 84/200 for
   `REVERSE_PROMPT.md`, 34/200 for `docs/decisions` and 19/200 for `src/`, a
   one-commit absorption is about as likely to touch no reach path as to touch one. **A
   conditional was stated as an unconditional**: the defensible form was "IF reach flips,
   it will most likely be `REVERSE_PROMPT.md` or `docs/decisions` — and it may well not
   flip at all." Absorption 70 confirming it made the phrasing look sounder than it was.

3. ⚠ **The ownership check is vacuous for the third absorption running** — root `src/` and
   `tests/` see **0** arriving files. Absorption 69 remains the only one in which it fired.

4. **The backend gate is unaffected**; no `native_codegen/` source arrives.

5. **Reach will name this brief and nothing else from the absorption.** `docs/decisions`
   is in the reach set, so filing the prediction moves reach while the absorption itself
   does not — the self-reference recorded twice before, arriving a third time.

## The narrower point

Two absorptions in a row now bear on one prediction: 70 confirmed it and 71 shows the
phrasing was over-committed. **One confirmation was treated as evidence for the method,
and the method's own figures said the claim was weaker than stated.** The measurement was
right; the sentence built on it was not.
