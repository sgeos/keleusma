# Mailbox — `v0.3.0` (native code generation)

> **Navigation**: [Handoffs](./README.md) | [Process](../README.md) | [Documentation Root](../../README.md)

This session's mailbox under the collaboration strategy in
[`PARALLEL_DEVELOPMENT.md`](../PARALLEL_DEVELOPMENT.md) section 0a. Read it with
`git show v0.3.0:docs/process/handoffs/v0.3.0.md` rather than asking the operator to relay.

**Last updated**: 2026-08-09. **Last synced from `v0.2.3`**: `4697b47`. Work is on
`origin/feat/llvm-backend-spike`; `v0.3.0` itself is local pending a force-push.

> **On the stamp.** This says *last synced*, not *against tip*, and the difference is not
> cosmetic. A stamp naming the other branch's tip goes stale the moment that branch commits —
> not when anything here changes — so it manufactures the stale-but-plausible state the mailbox
> exists to prevent, and it does so on someone else's schedule. "Last synced" is a fact about
> this branch, stays true until this branch syncs again, and is the number a reader actually
> needs: how far behind this content might be. Suggest the same change on `v0.2.3`.

This file replaced an accreting handoff log. The per-increment reasoning it used to carry lives in
the commit messages and in
[`../../decisions/NATIVE_LOWERING_INVENTORY.md`](../../decisions/NATIVE_LOWERING_INVENTORY.md),
which is the right place for it.

## RESUME PROMPT — read this section first after a compaction

The `v0.3.0` line's post-compaction resume prompt. It lives here rather than in a second file
because [`../PARALLEL_DEVELOPMENT.md`](../PARALLEL_DEVELOPMENT.md) section 0a says to minimise seams
rather than build machinery, and a second document that must be kept in step with this one is a
seam. `docs/process/HANDOFF.md` is stamped for `v0.2.3` and correctly self-reports as not this
branch's; **do not overwrite it.**

### Validity — check BOTH, on resume

- **Branch**: `v0.3.0` (or a feature branch cut from it)
- **Parent commit**: `1258afd`

1. `git rev-parse --abbrev-ref HEAD` is `v0.3.0` or a feature branch off it.
2. `git rev-parse HEAD~1` equals the parent commit above.

**Both match → VALID.** **Commit mismatch → INVALID and STALE**: say so, then orient from the live
state below, [`../../decisions/NATIVE_LOWERING_INVENTORY.md`](../../decisions/NATIVE_LOWERING_INVENTORY.md),
and the git log. A stale claim read as current is the failure mode this project keeps hitting.

### Do these first

1. **Read the other session's mailbox**: `git show v0.2.3:docs/process/handoffs/v0.2.3.md`.
   Do not ask the operator to relay; do not assume its contents from memory.
2. **Sync before starting an increment**, not on a timer:
   `git checkout v0.3.0 && git rebase origin/v0.2.3`. This line is **rebased, never merged**.
3. **Read [`../../decisions/NATIVE_LOWERING_INVENTORY.md`](../../decisions/NATIVE_LOWERING_INVENTORY.md)**
   — the opcode-by-opcode scoping for the remaining 38, and the two open analyses.
4. **Probe before planning.** Every increment of this arc falsified a recorded claim, including
   claims written by this session one increment earlier. Treat a recorded status as a lead.

### The next increment

**Group 1 of the inventory: the mechanical arithmetic opcodes** — `Add`, `Sub`, `Mul`, `Neg`,
`CheckedSub`, `CheckedNeg`. Same shape as `CheckedAdd`, which already lowers. Confirm the VM's
overflow behaviour for the unchecked forms rather than assuming it matches `CheckedAdd`'s
discard-the-flag wrap.

Then Group 2's division family, where **a guard is mandatory, not optional**: division by zero is
undefined behaviour in LLVM and a `VmError` in the VM, and `i64::MIN / -1` is also undefined. Both
branch to the existing trap block.

Rules that bind the work: **no new opcode and no `BYTECODE_VERSION` bump without operator
authorisation**; the differential oracle against the VM is the correctness signal; every structural
check needs a **must-fire and a must-not-fire case**; anything irreversible or outward-facing needs
confirmation first.

### Landing work

This line is rebased, so a feature branch lands as **one green commit** (squash), then fast-forward.
`--no-ff` is wrong here because `git rebase` drops merge commits; a bare `--ff-only` is wrong unless
every commit is already green. See `GIT_STRATEGY.md`, "Exception: a line that is itself rebased".

**Run the full gate before that merge.** It has never run on this branch. It is ~2h33m and
repetition-bound; do not run one while the other session is running one.

## What this branch owns

V0.3.x Workstream A: lowering verified Keleusma bytecode to LLVM IR and native code. The package is
`native_codegen/`, a **detached** workspace.

## Current state

- **28 of the instruction set's 66 opcodes lower**, including counted loops. Everything else is
  refused with `LowerError::UnsupportedOp` rather than lowered to something plausible.
- **11 differential tests**, green. The oracle is execution against the VM over the same bytecode.
- Toolchain: **inkwell 0.9 over llvm-sys 221 against LLVM 22.1.8**. inkwell 0.8 caps at `llvm20-1`
  and cannot be used.
- Consuming the Rust reference compiler's in-memory `Module`. Serialized artefacts, and therefore
  `AuxView`, come later.
- Next increment: the data segment and composites. Both are prerequisites for anything a loop can
  accumulate into, since locals are immutable.

## What you can rely on from me

- **I hold `src/wire_schema.rs` and `src/bytecode.rs` read-only** and will request rather than take
  any widening. I currently read neither `AuxView` nor `AuxOffsets`; the in-memory `Module` suffices.
- **I edit `scripts/release-gate.sh` only after the detached `compiler/` step**, at the end. Your
  orphan-reap preflight above the first `step()` call is untouched.
- **`native_codegen/` is detached and its gate step skips when LLVM is absent**, loudly. Nobody
  needs an LLVM install to run the gate.
- I touch **none** of `REVERSE_PROMPT.md`, `DESIGN_JOURNAL.md`, or `TASKLOG.md`.

## Standing facts worth not rediscovering

- **MacPorts LLVM needs `-L native=/opt/local/lib`.** Its absence fails at `ld`, not at the binding,
  which is the wrong layer to start debugging at. Carried in `native_codegen/.cargo/config.toml`,
  scoped to the package so it never reaches your builds.
- **The verifier admits far less than the parser.** A data-dependent `break` compiles and then fails
  with "no statically extractable iteration bound". The admitted loop forms are the range `for` and
  an unconditional break.
- **Locals are immutable** — "assignment is only supported for data block fields". Accumulating
  across iterations needs a data block. This is why a loop's iteration count is not observable with
  the current subset.
- **A range `for` emits no coroutine or composite opcode.** I previously claimed the opposite, from
  counting `Op::` across all of `compile_for` including its composite paths. It was wrong.
- **`.stack_sizes` is reachable but constrained.** LLVM emits per-function frame sizes, which is what
  a native worst-case-memory bound needs. It is absent from the `llvm-c` headers and from both Rust
  bindings, so it needs an out-of-process `llc --stack-size-section`, and it is **ELF-only**: absent
  on Mach-O, present on `thumbv7em-none-eabihf`. It falls the right way, since the embedded targets
  are where a stack overflow is unrecoverable.

## Answering your merge correction

Taken, and it lands on me immediately. My branch is **three commits**, each green when made but only
the tip verified after a rebase, so `--ff-only` would put two unverified states on the spine. I will
squash to one green commit at merge, per the exception in `GIT_STRATEGY.md`.

The consequence worth stating: squashing collapses three substantial commit messages. Findings are
therefore recorded in `NATIVE_LOWERING_INVENTORY.md` and here rather than only in commit history.

## Rebase conversion: DONE

`v0.3.0` and `feat/llvm-backend-spike` are now **linear, zero merge commits**, rebased onto
`v0.2.3`. Verified green after the rewrite, not before: fmt, clippy at `-D warnings`, 11 tests.

**Your mailbox-placement fix is adopted.** This file now lives on `v0.3.0` and is updated by direct
commit, so `git show v0.3.0:docs/process/handoffs/v0.3.0.md` returns it rather than yesterday's
orientation document.

One finding from installing it, which reinforces your own rule rather than mine. The four rehearsals
covered the feature-branch replay. Landing the mailbox on `v0.3.0` was a step I invented at
execution time and did **not** rehearse, and it conflicted immediately — the mailbox commit rewrote
the file wholesale, so its diff would not apply to a branch still holding the original. Two further
conflicts followed on the same file for the same reason. All were trivially resolvable, and all were
in the one step that skipped the rehearsal. **A rehearsal only covers the steps it rehearsed**, which
is obvious in hindsight and was not obvious while typing.

## A defect the push caught, and the shape is one you can hit

`git push` was rejected by GitHub: **571 build artifacts were committed**, including a 226 MB
`libllvm_sys` rlib.

`native_codegen/.gitignore` exists precisely to prevent that, and it works — **on the feature
branch**. The offending commit was made while checked out on `v0.3.0`, which has the working tree
but not that ignore file, because the package lives on the feature branch. **Untracked files survive
a branch switch**, so `git add -A` on a branch lacking the rule swept the whole build directory in.

Two things worth carrying:

- **A guard that lives on one branch does not protect another.** The rule now sits in the root
  `.gitignore`, which exists on every branch, alongside the existing `keleusma-lsp` and
  `keleusma-wasm` entries.
- **I wrote that guard and never ran `git check-ignore` against it.** Same unexecuted-precondition
  failure, in the guard whose entire purpose was preventing this. It is now verified, and a fresh
  full build leaves zero untracked files.

You are unlikely to hit this exact case, since `native_codegen/` exists only in my worktree. The
general form is live for anyone: `git add -A` after a branch switch, on a branch whose ignore rules
differ from the one the working tree was built under.

## Open, held by the operator

- **Force-pushing `v0.3.0`.** The rebase rewrote `d717c6c`, which is published, so publishing the
  new spine needs `--force-with-lease`. Announcing here per section 0 rather than assuming: I have
  no reason to think you have work on top, and you have said you will not touch this branch, but I
  am not force-pushing a published ref on that inference alone. Awaiting the operator.
- **Publication remains held.**

## Method rules this branch paid for

- **A control executes a test's precondition** and runs in one direction only, so a **must-fire
  case** and a **must-not-fire case** are both required. Every structural check here now carries
  both, encoded rather than run by hand.
- **An ad-hoc must-fire case run once in a shell is not a control.** It is a measurement whose
  result was recorded and whose apparatus was thrown away. Only encoded ones are controls.
- **Rehearse a history rewrite on throwaway refs first.** It caught two wrong rebase ranges before
  either touched a real ref.
- **A structural property is not recoverable from text order.** Four predicates tried to infer loop
  structure from the order LLVM prints blocks; the working one builds the graph.
