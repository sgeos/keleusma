# Task Log

> **Navigation**: [Process](./README.md) | [Documentation Root](../README.md)

Current sprint source of truth.

---

## Current Phase

**V0.1**: Embedding-grade runtime. Precompiled bytecode loading and trust-based verification skip in place.

## Active Milestone

V0.1-M1 complete. Precompiled bytecode wire format with magic and version header, deserialization from any addressable byte slice including .rodata, and unsafe trust-skip API for hosts that pre-verify bytecode at build time. R39 records the design.

## Task Breakdown

| ID | Description | Status | Verification |
|----|-------------|--------|--------------|
| V0.0-M0-T1 | Extract crate from workspace | Complete | cargo test passes |
| V0.0-M0-T2 | Create knowledge graph | Complete | All docs files present |
| V0.0-M1-T1 | Block-structured ISA transition | Complete | R22 |
| V0.0-M1-T2 | Productivity verification and WCET analysis | Complete | R23 |
| V0.0-M2-T1 | For-in over arrays, tuple literals, utility natives | Complete | 216 tests pass, zero clippy warnings |
| V0.0-M2-T2 | Formal related work and citations across knowledge graph | Complete | RELATED_WORK.md present, citations applied to LANGUAGE_DESIGN, EXECUTION_MODEL, TARGET_ISA, GRAMMAR, GLOSSARY |
| V0.0-M3-T1 | Data segment design specification | Complete | R24 through R28 |
| V0.0-M3-T2 | Data segment formalization pass | Complete | RELATED_WORK Section 8, citations across architecture and design docs |
| V0.0-M3-T3 | Data segment partial implementation | Complete | Parser, AST, bytecode, compiler, VM scaffolding for data blocks |
| V0.0-M3-T4 | Singular data block enforcement | Complete | Compile-time error referencing R28, test coverage |
| V0.0-M3-T5 | Fixed-size field type enforcement | Complete | Recursive validation at data block declaration boundary, test coverage for primitives, tuples, arrays, options, structs, enums, and rejection cases |
| V0.0-M3-T6 | Verifier bounds checking for GetData and SetData | Complete | Slot index validated against data layout slot count, test coverage |
| V0.0-M3-T7 | Host interoperability layer specification | Complete | R29. Slot-based Vec<Value> interface. Documented in EXECUTION_MODEL and COMPILATION_PIPELINE |
| V0.0-M3-T8 | End-to-end data segment integration tests | Complete | Six hot swap tests added covering same-schema, new-schema, size mismatch, no-data module, swap at reset, and rollback. 238 tests pass, zero clippy warnings |
| V0.0-M3-T9 | Hot swap API replace_module on Vm | Complete | replace_module, data_len added with documentation |
| V0.0-M3-T10 | Concurrency contract specification | Complete | Single-ownership enforced by Rust borrow checker. Documented in EXECUTION_MODEL |
| V0.0-M4-T1 | Cargo workspace conversion with keleusma-macros | Complete | Workspace member added, runtime crate retains src/ at root |
| V0.0-M4-T2 | KeleusmaType trait and primitive impls | Complete | Trait in src/marshall.rs covers i64, f64, bool, (), Option, fixed-arity tuples through 4, and fixed-length arrays |
| V0.0-M4-T3 | KeleusmaType derive for structs and enums | Complete | Named-field structs and enums with unit, tuple, and struct-style variants |
| V0.0-M4-T4 | IntoNativeFn family and register_fn API | Complete | Arities 0 through 4 for both infallible and Result-returning host functions |
| V0.0-M4-T5 | Migrate audio_natives and utility_natives to register_fn | Complete | Twelve functions migrated; the three Value-polymorphic functions remain on register_native |
| V0.0-M4-T6 | Integration tests for derive and register_fn | Complete | tests/marshall.rs adds 17 integration tests |
| V0.0-M4-T7 | Documentation pass for marshalling layer | Complete | LANGUAGE_DESIGN, COMPILATION_PIPELINE, GLOSSARY, RELATED_WORK Section 9, R30, B5 |
| V0.0-M5-T1 | Record R31 R32 R33 and B6 B9 B10 | Complete | Decisions and backlog entries documented |
| V0.0-M5-T2 | Two-string-type discipline at runtime | Complete | Value::StaticStr and Value::DynStr distinct variants. Source literals compile to StaticStr. to_string returns DynStr. String concat result is DynStr. |
| V0.0-M5-T3 | Cross-yield prohibition on dynamic strings | Complete | Runtime structural check at Op::Yield. Value::contains_dynstr helper. Tests for static yield, dynamic yield rejection, tuple-with-dynstr rejection. |
| V0.0-M5-T4 | Documentation pass for fifth guarantee | Complete | TYPE_SYSTEM, LANGUAGE_DESIGN, EXECUTION_MODEL, GLOSSARY updated. Five guarantees explicit. Dual-end arena documented. |
| V0.0-M5-T5 | Arena allocator with allocator-api2 | Foundation complete in V0.0-M6 | See V0.0-M6 below. |
| V0.0-M5-T6 | WCMU instrumentation | Deferred | Tracked as P8. Pairs with the deeper arena integration. |
| V0.0-M6-T1 | Add allocator-api2 dependency | Complete | Cargo.toml updated, no_std + alloc preserved |
| V0.0-M6-T2 | Implement Arena type | Complete | src/arena.rs with dual-end bump pointers, alignment-aware allocation, reset method |
| V0.0-M6-T3 | Implement Allocator trait for handles | Complete | StackHandle and HeapHandle types implementing allocator_api2::Allocator |
| V0.0-M6-T4 | Wire arena into Vm | Complete | Arena field, configurable capacity, reset on Op::Reset and replace_module |
| V0.0-M6-T5 | Documentation pass | Complete | R34 added, EXECUTION_MODEL updated, GLOSSARY updated, P7 marked foundation-complete |
| V0.0-M6-T6 | Operand stack and DynStr arena migration | Complete | Resolved by V0.1-M3-T11. Operand stack migrated to `allocator_api2::vec::Vec<T, BottomHandle>`. `Value::KStr(KString)` is the arena-allocated dynamic string boundary type. |
| V0.0-M6-T7 | WCMU instrumentation | Complete | Op::stack_growth, Op::stack_shrink, Op::heap_alloc methods. wcmu_stream_iteration in verify.rs. verify_resource_bounds called from Vm::new and Vm::replace_module |
| V0.0-M6-T8 | Native attestation API for WCET and WCMU | Complete | Vm::set_native_bounds. R35 records the implementation |
| V0.0-M6-T9 | Tests for WCMU analysis and verification | Complete | Eight new tests covering simple stream, branching, NewStruct heap, NewArray heap, non-stream rejection, resource bounds pass, oversized rejection, and non-stream skip |
| V0.0-M6-T10 | Auto-arena sizing | Complete | Resolved by P8 (`auto_arena_capacity_for`). Hosts pre-size the arena before VM construction; the convenience `Vm::new_auto` was rolled into the host-owned-arena constructor in V0.1-M3-T10. |
| V0.0-M6-T11 | Call-graph WCMU integration | Complete | Resolved by P8 (`module_wcmu` walks the call graph topologically with native attestation propagation). |
| V0.0-M6-T12 | keleusma-arena pre-publication polish | Complete | crates.io name available. Drop impl audit comment in source. MSRV verified at 1.85. miri stacked-borrows clean (21 of 22 tests; one ignored due to deliberate Vec leak). 16-byte aligned heap allocation via alloc_zeroed and matching dealloc on drop. Non-global-allocator note in Arena type doc. CHANGELOG.md following Keep a Changelog conventions. mixed_allocator example demonstrating arena alongside global allocator. cargo publish --dry-run succeeds. |
| V0.0-M6-T13 | keleusma-arena pre-publication final pass | Complete | Tree borrows verified (21 of 22 miri tests pass, same single deliberate-leak ignore). docs.rs metadata block added (all-features, rustdoc-args=docsrs). Doctest added on Arena::with_capacity demonstrating construction, allocator-api2 integration, and observability. CI extended with miri (stacked and tree borrows), MSRV pin (1.85, default and no-default-features), and clippy --workspace --all-targets. |
| V0.0-M6-T14 | keleusma-arena docs polish | Complete | doc_cfg annotation added to Arena::with_capacity so docs.rs renders the alloc feature badge. README wired into crate-level documentation via include_str so all README code blocks run as doctests. Static-Buffer Use example rewritten to use addr_of_mut for edition 2024 compatibility. Existing structured reference content preserved as ## API Reference subsection following the README. |
| V0.0-M6-T15 | keleusma-arena published to crates.io v0.1.0 | Complete | cargo publish succeeded. 12 files, 55.1 KiB total, 15.3 KiB compressed. Crate listing live at https://crates.io/crates/keleusma-arena. Documentation rendering at https://docs.rs/keleusma-arena/0.1.0/. |
| V0.0-M6-T16 | Switch keleusma to registry version of keleusma-arena | Complete | Cargo.toml dependency line for keleusma-arena dropped path attribute and now uses version "0.1" only. Cargo.lock updated to consume keleusma-arena v0.1.0 from crates-io. Workspace tests pass (323 total). keleusma-arena remains a workspace member for convenient local development of the arena crate, but keleusma's dependency now resolves through the registry. |
| V0.1-M1-T1 | Wire format for precompiled bytecode | Complete | postcard 1.x and serde 1.x added. Serialize and Deserialize derived on Module, Chunk, Op, Value, BlockType, StructTemplate, DataSlot, and DataLayout. BYTECODE_MAGIC ("KELE") and BYTECODE_VERSION (1) constants. Module::to_bytes and Module::from_bytes with header validation. LoadError enum with BadMagic, Truncated, UnsupportedVersion, and Codec variants. |
| V0.1-M1-T2 | Trust-based verification skip API | Complete | unsafe Vm::new_unchecked and unsafe Vm::new_unchecked_with_arena_capacity skip the resource bounds check while keeping structural verification active. Convenience constructors Vm::load_bytes and unsafe Vm::load_bytes_unchecked compose deserialization with new and new_unchecked. VmError::LoadError variant added with From conversion from bytecode::LoadError. |
| V0.1-M1-T3 | Tests for precompiled bytecode | Complete | Roundtrip compile-serialize-deserialize-run produces identical results. Bad magic rejected with BadMagic. Short input rejected with Truncated. Wrong version rejected with UnsupportedVersion. Vm::load_bytes propagates LoadError as VmError::LoadError. unsafe new_unchecked admits a module that fails bounds verification with a tiny capacity. unsafe new_unchecked still rejects modules that fail structural verification (Stream chunk without yield). 7 new tests, 286 total in keleusma. |
| V0.1-M1-T4 | Knowledge graph documentation | Complete | R39 records the wire format design and trust-skip rationale in RESOLVED. P10 records zero-copy execution as the deferred path B in PRIORITY. B9 and B10 in BACKLOG cross-reference R39 for the addressed portions. EXECUTION_MODEL gains a Bytecode Loading section between Memory Model and Hot Code Swapping. |
| V0.1-M1-T5 | CRC-32 algebraic self-inclusion trailer | Complete | Hand-rolled bit-by-bit CRC-32 added to bytecode.rs (15 lines). Standard IEEE 802.3 polynomial 0xEDB88320, init/xorout 0xFFFFFFFF, refin/refout true. Residue constant 0x2144DF1C. Module::to_bytes appends the CRC as a four-byte little-endian trailer. Module::from_bytes runs CRC over the entire slice in one pass and compares against the residue. LoadError gains BadChecksum variant. Validation order is truncation, magic, CRC, version, body decode. R39 and EXECUTION_MODEL updated. Two new tests cover the residue property against the reference value crc32("123456789") = 0xCBF43926 and bad-checksum rejection. 288 keleusma unit tests, 333 workspace total. |
| V0.1-M1-T6 | Extended header with length, word size, address size, golden test | Complete | BYTECODE_VERSION bumped to 2. Header layout grew to twelve bytes adding a 32-bit little-endian total length field, an 8-bit word_bits field, and an 8-bit addr_bits field. RUNTIME_WORD_BITS=64 and RUNTIME_ADDRESS_BITS=64 record the current target. LoadError gains WordSizeMismatch and AddressSizeMismatch variants. Recorded length is authoritative; trailing bytes after the recorded length are ignored, supporting bytecode embedded in a larger buffer. Validation order is truncation, magic, length, CRC residue, version, word size, address size, body decode. Six new tests cover word size mismatch, address size mismatch, oversized length field, undersized length field, trailing padding admission, and golden-bytes pinning. The golden test hardcodes the exact 37-byte serialization of `fn main() -> i64 { 1 }` to catch wire format drift and endian regressions. R39 and EXECUTION_MODEL updated. 294 keleusma unit tests, 339 workspace total. |
| V0.1-M1-T7 | Word/address as log2 exponent; relaxed policy; integer masking | Complete | BYTECODE_VERSION bumped to 3. Word and address fields encoded as base-2 exponents (RUNTIME_WORD_BITS_LOG2 = 6 and RUNTIME_ADDRESS_BITS_LOG2 = 6 for 64-bit). Acceptance policy relaxed to bytecode-exponent <= runtime-exponent. Module gains word_bits_log2 and addr_bits_log2 fields with #[serde(skip)]. truncate_int helper applies sign-extending mask when narrower-than-runtime. VM applies truncation in Add, Sub, Mul, Div, Mod, Neg integer paths. LoadError variants renamed `expected` -> `max_supported` to reflect <=. Two new tests added: bytecode_admits_narrower_word_size confirms 32-bit declared bytecode runs on 64-bit runtime, bytecode_masking_truncates_to_declared_width confirms `2147483647 + 1` produces -2147483648 under 32-bit declared width. Golden bytes updated. 296 keleusma unit tests, 341 workspace total. |
| V0.1-M2-T1 | P10 Phase 1. Switch body format from postcard to rkyv | Complete | BYTECODE_VERSION bumped to 4. serde and postcard dependencies replaced with rkyv 0.8 (alloc + bytecheck features). All bytecode types gain rkyv Archive, Serialize, and Deserialize derives. Recursive Value uses #[rkyv(omit_bounds)] on self-referential fields and explicit serialize_bounds, deserialize_bounds, and bytecheck bounds. HEADER_LEN raised to 16 with four reserved bytes that pad the header so the body begins at an 8-byte-aligned offset. Minimum framing size now 20 bytes. Module::from_bytes copies the body to AlignedVec<8> for alignment and calls rkyv::from_bytes. Module::to_bytes uses rkyv::to_bytes. Phase 2 (zero-copy execution against borrowed buffer) is deferred to V0.1-M2-T2. 296 keleusma unit tests, 341 workspace total. Golden bytes updated to record the new 144-byte serialization. |
| V0.1-M2-T2 | P10 Phase 2 step 1. Aligned-input zero-copy validation | Complete | Module::access_bytes returns &ArchivedModule for aligned input through rkyv::access. Module::view_bytes uses access_bytes and rkyv::deserialize without the AlignedVec copy that from_bytes performs. Vm::view_bytes and unsafe Vm::view_bytes_unchecked compose validation with the safe and unchecked Vm constructors. Three new tests cover aligned input via AlignedVec, unaligned input rejection, and direct ArchivedModule inspection. The execution loop continues to operate on the deserialized owned Module. 299 keleusma unit tests, 344 workspace total. |
| V0.1-M2-T3 | P10 Phase 2 step 2. Conversion helpers for archived bytecode | Complete (foundation) | Op derives Copy. bytecode::op_from_archived covers all 48 variants with to_native conversions for endian-explicit fields. bytecode::value_from_archived covers all 11 Value variants recursively. Round-trip tests confirm preservation of op and value identity across archive serialization. Foundation for the execution loop rewrite. 301 keleusma unit tests, 346 workspace total. |
| V0.1-M3-T47 | Documentation pass for the SDL3 example | Complete | Documentation gaps for the `piano_roll` example identified and closed. The top-level `README.md` Examples section was updated to mention hot code swap as one of the capabilities the example exercises and to document the `s` and Enter keyboard controls explicitly. The `docs/README.md` Quick Reference table gained three rows pointing at `examples/scripts/`, `examples/`, and `examples/piano_roll.rs` so a user starting from the documentation knowledge graph reaches the example. The `docs/guide/EMBEDDING.md` Cross-References section gained a piano_roll entry describing the example's coverage. The Hot Code Swapping subsection in EMBEDDING.md had two correctness issues fixed: the `Vm::replace_module` signature requires both a module and an `initial_data: Vec<Value>` argument (the prior snippet omitted the data argument), and after a swap the host must call `Vm::call` to start the new module's entry point, not `Vm::resume` (the prior snippet incorrectly called resume). Both bugs in the doc were latent before the example exercised the code path; the example surfaced them. The guide's GETTING_STARTED.md "Next Steps" list and the docs/guide/README.md "Companion Material" table both gained piano_roll entries with the run command and keyboard controls. 519 tests pass workspace-wide; documentation-only change. |
| V0.1-M3-T46 | Hot code swap demonstration in the SDL3 example | Complete | The `piano_roll` example now exercises `Vm::replace_module` for hot code reload between two songs. Two precompiled `Module` values (`module_a` from `piano_roll.kel`, `module_b` from a new `piano_roll_2.kel`) sit in main memory at startup. The stdin reader thread parses each line and forwards either `Command::Swap` (for input "s") or `Command::Quit` (for any other line) over an `mpsc` channel. The main tick loop drains the channel non-blocking each iteration; on receipt of `Swap` it flips a `swap_pending` flag and waits for the next `VmState::Reset` boundary, where it calls `vm.replace_module(next, fresh_data())` followed by `vm.call(...)` to drive the new module to its first yield. The data segment is reinitialised to all zeros at swap so the new song begins at the start of its phrase. Audio rendering continues uninterrupted across the swap; voices are explicitly silenced post-swap so the new song does not inherit pitches it never set. The new `piano_roll_2.kel` reorders the channel-to-musical-role mapping (channel 1 is harmony in song 2 versus bass in song 1; channel 2 is bass in song 2 versus harmony in song 1) so the swap also produces a deliberate timbral shift on the fixed host instrument table. Banner updated to "[ Press 's' + Enter to swap song. Press Enter alone to quit. ]". Verified end to end: pressing `s` produces "[ swapped to song 2 ]", pressing `s` again produces "[ swapped to song 1 ]", pressing Enter exits. Workspace builds and tests remain SDL3-free; the feature-gated example builds and runs cleanly. 519 tests pass workspace-wide; clippy clean. |
| V0.1-M3-T45 | Refactor SDL3 example to gated Cargo example | Complete | The `keleusma-piano-roll` workspace member was refactored into a feature-gated Cargo example to align with idiomatic Rust packaging. The example now lives at `examples/piano_roll.rs` plus `examples/piano_roll.kel`. SDL3 is declared as an optional dependency in the parent `keleusma` crate behind the `sdl3-example` feature. The `[[example]]` entry uses `required-features = ["sdl3-example"]`. Standard `cargo build --workspace` and `cargo test --workspace` no longer pull in SDL3 (verified: workspace build completes without compiling sdl3 or sdl3-sys). The example builds and runs through `cargo run --release --example piano_roll --features sdl3-example`. Without the feature, Cargo prints an actionable error pointing the user at the missing feature. The `keleusma-piano-roll/` directory was removed; the architecture and "why this example" notes from its README were folded into the file-level doc comment block on `examples/piano_roll.rs`. The `package.metadata.docs.rs` block dropped its `all-features = true` entry to keep docs.rs builds SDL3-free. 519 tests pass workspace-wide; clippy clean on both the workspace and the feature-gated example. |
| V0.1-M3-T44 | SDL3 audio example: keleusma-piano-roll | Complete | New `keleusma-piano-roll` workspace member providing a three-channel piano-roll example. SDL3 audio host (`src/main.rs`, ~250 lines) driven by a Keleusma tick-based control loop (`song.kel`, ~200 lines). Demonstrates the synchronous-reactive split: musical logic at tick rate (120 BPM 16th notes = 125 ms per tick), sample synthesis at sample rate (48 kHz), thread-safe handoff via `Arc<Mutex<[Voice; 3]>>`. Three voices: square-wave melody, triangle-wave bass, square-wave harmony, playing a four-bar I-vi-IV-V progression in C major (C-Am-F-G) that auto-loops. Per-channel instrument parameters (waveform, volume) live as `const` arrays at the top of the Rust file; the song lives entirely in the Keleusma file as hard-coded note arrays of `(Pitch, octave, duration_in_16ths)` tuples. Note enum carries the twelve chromatic pitch classes plus `Rest`. Per-channel position state `(idx_n, rem_n)` lives in the data segment so the script resumes cleanly across ticks. Two natives: `host::play(ch, midi)` and `host::silence(ch)` update shared voice state. Stdin reader thread sets an `AtomicBool` quit flag on Enter. SDL3 builds from source through the `build-from-source-static` feature so the crate is self-contained at the cost of a one-time CMake build. End-to-end smoke-tested: program compiles and verifies the script, opens the SDL3 audio device, drives the tick loop, and exits cleanly on stdin input. Bug fixes during authoring: data block syntax requires a name (`data state { ... }` not `data { ... }`, also fixed in WHY_REJECTED.md), `use` declarations must precede everything else, if-else expressions at statement position require explicit trailing semicolons. 519 tests pass workspace-wide; clippy clean. |
| V0.1-M3-T43 | Onboarding documentation: docs/guide/ and examples/scripts/ | Complete | New `docs/guide/` section closes the onboarding gap relative to comparable scripting languages such as Rhai. Three guide documents: `GETTING_STARTED.md` (install the CLI, write a first script, embed in a twenty-line Rust host), `EMBEDDING.md` (VM lifecycle, native function registration including `register_fn`, `register_fn_fallible`, `KeleusmaType` derive, arena sizing including `auto_arena_capacity_for` and static-buffer arenas, call and resume protocol with `VmState` enumeration, error recovery via `reset_after_error`, hot code swapping, trust-skip construction), `WHY_REJECTED.md` (verifier rejection taxonomy with first vs second category, error-message-to-root-cause mapping for `MakeRecursiveClosure`, `CallIndirect`, loop-bound extraction, recursive call detection, missing yield, resource bounds exceeded, with rewrite suggestions). Eight standalone `.kel` scripts under `examples/scripts/`: arithmetic, struct field access, enum match, for-in iteration (range and array), pipeline operator, multiheaded function dispatch, f-string interpolation, trait method dispatch. Each script documented with expected output and runs end to end through `keleusma run`. Verified outputs: 24, 7, 100, 40, 169, 11, "hello, Keleusma! 7 plus 2 is 9", 84. `docs/README.md` and top-level `README.md` updated to surface the guide section. The fold-native rewrite suggestion in `WHY_REJECTED.md` documents the immutable-locals constraint and the data-segment versus host-fold options for accumulation. 519 tests pass workspace-wide; format and clippy clean. Documentation-and-examples-only change, no source modifications. |
| V0.1-M3-T42 | Standalone CLI: keleusma compiler, runner, REPL | Complete | New keleusma-cli workspace member providing the `keleusma` binary, modeled after Rhai's CLI ergonomics. Subcommands: `run <file>` parses, compiles, verifies, and executes a script through the safe constructor with utility and math natives pre-registered; `compile <file> [-o <output>]` produces a serialized bytecode file consumable by `Vm::load_bytes`; `repl` opens an interactive prompt that distinguishes declarations (fn, yield, loop, struct, enum, trait, impl, use, data) from expressions, accumulates declarations into a session prefix, and evaluates expressions by wrapping them in `fn main() -> T` while iterating return-type candidates i64, f64, bool, String, () in order. Shorthand: any first argument ending in `.kel` is treated as `run`. REPL commands `:help`, `:quit`, `:reset`, `:show`. Smoke-tested: `keleusma run hello.kel` prints 42; `keleusma compile hello.kel -o tmp/hello.kel.bin` writes 220 bytes; REPL session defines and invokes a function correctly. Limitations documented: yield/resume not driven interactively, REPL data-segment values not persisted across evaluations, fixed return-type list, no cross-target compilation. Installation through `cargo install --path . --bin keleusma`. 0BSD license. 519 tests pass workspace-wide; format and clippy clean. |
| V0.1-M3-T9 | Type checker gaps 11-14 closed | Complete | Multiheaded parameter types tracked through compile_pattern_bind_typed. The compiler's TypeInfo gained an enums map. Native call checking distinguishes user functions, declared natives via use, qualified-name natives, and undefined names (now rejected). Pattern type checking validates tuple arity, enum variant existence and payload arity, struct field names, and literal compatibility against the scrutinee. Match exhaustiveness covers enum (all variants or wildcard), bool (true and false or wildcard), unit (() or wildcard), and other types (wildcard required). 15 new tests across the four areas. 344 keleusma unit tests, 389 workspace total. |
| V0.1-M3-T8 | Tests for match expressions inside nested for-in | Complete | Two new tests confirm match expressions work inside three-level nested for-in loops. match_on_inner_value_inside_three_level_nested_for_in matches on the innermost iteration variable value and accumulates differing contributions. tuple_match_across_three_loop_variables uses three independent 1D arrays and matches on the tuple (x, y, z) to detect the origin cell. Both produce the expected sum 107. 329 keleusma unit tests, 374 workspace total. |
| V0.1-M3-T7 | P2. Iteration variable type tracking for nested for-in | Complete | The for-in iteration variable now records the element type derived from the source's array type. Previously the iteration variable carried no type, so nested for-in over a multi-dimensional array could not resolve its bound past the outer level. With type tracking on the iteration variable, each nested level reads the parent variable's element type and emits a Const(N) end bound. Two new tests confirm the verify path and end-to-end execution of a three-level nested for-in over a [[[i64; 2]; 2]; 2] matrix. The runtime test sums 1 through 8 to 36 by accumulating in the data segment across all 8 visits. 327 keleusma unit tests, 372 workspace total. |
| V0.1-M3-T6 | P2. For-in over nested array indexing and match expression results | Complete | infer_expr_type extended with Expr::ArrayIndex (extract element type from object's array type) and Expr::Match (use first arm's expression type). element_type_of helper added. static_for_in_length now handles ArrayIndex and Match patterns directly. Two new tests pass: for_in_over_nested_array_index_passes_strict_verify and for_in_over_match_array_result_passes_strict_verify. P2 has no remaining deferred cases. 325 keleusma unit tests, 370 workspace total. |
| V0.1-M3-T5 | P2. Local type tracking for for-in struct field access | Complete | Local struct gains a ty: Option<TypeExpr> field. Let bindings record declared annotation or inferred type via new infer_expr_type helper covering struct construction, function calls, identifiers, field access, array literals, and literal values. Parameters record their declared type at function entry through the new bind_param_pattern signature. static_for_in_length now consults Expr::Ident through the local table. Two new tests pass: for_in_over_struct_field_from_local_passes_strict_verify and for_in_over_param_array_passes_strict_verify. P2 fully resolved for the practical cases. Nested array indexing and match expression results remain deferred. 323 keleusma unit tests, 368 workspace total. |
| V0.1-M3-T4 | P2. For-in over typed expressions | Complete (typed cases) | Compiler infers static array length from declared types and emits Const(N) for the for-in end bound. Cases now admissible under strict-mode WCMU: array literal, let-bound array literal (already worked), function return, data segment field. New TypeInfo struct in the compiler holds struct field types, function return types, and data field types collected from the AST at compile time. FuncCompiler::static_for_in_length helper performs the inference. Three new tests for the typed paths. The remaining case (struct field access from a local variable) requires local type tracking in the compiler and is recorded as future work. 321 keleusma unit tests, 366 workspace total. |
| V0.1-M3-T3 | P3. Error recovery model | Complete | Vm::reset_after_error clears volatile state (operand stack, frames, arena) and preserves the data segment. Hosts call this after an Err from call or resume to return the VM to a clean callable state. Data persists across error events, supporting long-running streams that accumulate state. Three unit tests cover the recovery path: reset_after_error_preserves_data, reset_after_trap_clears_volatile_state, reset_after_error_idempotent. P3 entry in PRIORITY marked resolved with the recovery contract documented. 318 keleusma unit tests, 363 workspace total. |
| V0.1-M3-T2 | P1. Type checker integrated into compile pipeline | Complete | Added Literal::Unit AST variant. Parser emits Unit for `()` literal. Compiler emits Op::PushUnit for Literal::Unit. Type checker recognizes Literal::Unit as Type::Unit. typecheck::check is now invoked from compile and errors are surfaced as CompileError. Five existing tests updated to declare their referenced struct and enum types: compile_enum_variant, compile_struct_init, compile_tuple_literal, eval_struct_init_and_field, eval_enum_variant. 315 keleusma unit tests, 360 workspace total. |
| V0.1-M3-T1 | P1. Standalone type checker | Complete | New src/typecheck.rs with Type, TypeError, Ctx, and the check entry point. Two-pass design collects type definitions and function signatures first, then checks each function body. Coverage includes function signatures, let bindings, arithmetic, comparisons, field access, struct construction, casts, identifier resolution, if branch agreement, for-range and for-in, enum variants, logical operators, and array and tuple indexing. 13 unit tests pass. The pass is not yet wired into compile because the parser currently represents the unit literal as Literal::Int(0). Wiring is the natural follow-up after a small parser change. 315 keleusma unit tests, 360 workspace total. |
| V0.1-M2-T4 | P10 Phase 2 step 2. Execution loop refactor against ArchivedModule | Complete | Vm gained lifetime parameter Vm<'a> with BytecodeStore<'a> carrying Owned AlignedVec or Borrowed slice. Execution loop rewritten to read via archived() helper and per-access converters (chunk_op, chunk_const, chunk_const_str, struct_template, native_name, chunk_op_count, chunk_local_count, word_bits_log2). Cold-path methods (verify_resources, auto_arena_capacity) deserialize to owned Module on call. replace_module serializes new module to AlignedVec. unsafe Vm::view_bytes_zero_copy is the true zero-copy constructor. New test vm_view_bytes_zero_copy_executes_against_borrowed_buffer confirms a program runs end-to-end against a borrowed AlignedVec slice with no owned Module materialized. register_utility_natives, register_audio_natives, and the marshall test harness updated for the lifetime parameter. 302 keleusma unit tests, 347 workspace total. |

## History

| Date | Summary |
|------|---------|
| 2026-03-02 | Crate extracted from Vows of Love and War workspace. Knowledge graph created. |
| 2026-03-02 | Block-structured ISA transition complete. R22. |
| 2026-05-08 | Productivity verification and WCET analysis complete. R23. |
| 2026-05-08 | For-in over arrays, tuple literals, utility natives, README. V0.0-M2 complete. 216 tests passing. |
| 2026-05-08 | Formal related work pass with citations across knowledge graph. RELATED_WORK.md created. |
| 2026-05-08 | Data segment design feasibility analysis. R24 through R28 added. RELATED_WORK Section 8 added covering hot code update with persistent state. |
| 2026-05-08 | Data segment source conformance brought into alignment with specification. Singular block check, fixed-size field type validation, verifier bounds check. 232 tests pass, zero clippy warnings. |
| 2026-05-08 | V0.0-M3 complete. Hot swap API added with replace_module and data_len. Six hot swap integration tests added. R29 records the slot-based interoperability decision. 238 tests pass. |
| 2026-05-08 | V0.0-M4 complete. Workspace conversion. keleusma-macros proc-macro crate added. KeleusmaType trait, derive, IntoNativeFn family, and register_fn API. Audio and utility natives migrated. 268 tests pass. R30 records the static marshalling decision. |
| 2026-05-08 | V0.0-M5 partial complete. Two-string-type discipline at runtime with Value::StaticStr and Value::DynStr. Cross-yield prohibition enforced. R31 R32 R33 added. WCMU and dual-end arena documented as the fifth guarantee. 272 tests pass. Arena allocator and WCMU instrumentation deferred to V0.0-M6 as P7 and P8. |
| 2026-05-08 | V0.0-M6 partial complete. Arena allocator foundation in place. allocator-api2 dependency added. Arena type with dual-end bump pointers, StackHandle and HeapHandle implementing allocator_api2::Allocator. Vm holds Arena, reset on Op::Reset and replace_module. R34 records the implementation. 286 tests pass. Operand stack and DynStr arena migration tracked as P7 follow-on. WCMU instrumentation tracked as P8. |
| 2026-05-08 | V0.0-M6 substantially complete. WCMU instrumentation added with per-op stack and heap cost methods. wcmu_stream_iteration parallels wcet_stream_iteration. verify_resource_bounds enforces stack_wcmu plus heap_wcmu fits within arena_capacity at Vm::new and Vm::replace_module. Native function attestation widened to include WCET and WCMU bounds via Vm::set_native_bounds. R35 records the implementation. 294 tests pass. Auto-arena sizing and call-graph integration deferred to V0.0-M7 as P8 follow-on. |
| 2026-05-08 | V0.0-M6 complete. Arena extracted to standalone keleusma-arena crate. Three constructors (with_capacity, from_static_buffer, from_buffer_unchecked). Renamed handles to BottomHandle and TopHandle. Added Budget contract for generic budget verification. Added mark and rewind API with unsafe per-end reset. Added peak watermark tracking. core-only operation without alloc supported. R36 records the extraction. 300 tests across the workspace. Tagline "Simple and boring memory allocator for exciting applications." |
| 2026-05-08 | P8 complete. Call-graph integration via module_wcmu walks DAG topologically. Native attestation propagation. Vm::new_auto auto-sizes arena. Vm::verify_resources re-checks with current attestations. Three keleusma examples added (wcmu_basic, wcmu_attestation, wcmu_rejection). R37 records the work. 310 tests pass. |
| 2026-05-08 | P9 complete. Bounded-iteration loop analysis. extract_loop_iteration_bound pattern-matches the canonical for-range bytecode shape. WCMU heap and WCET cost multiplied by iteration count for literal bounds. Conservative one-iteration fallback for non-literal bounds. R38 records the implementation. 315 tests pass. |
| 2026-05-08 | keleusma-arena pre-publication polish complete. Storage migrated from `Box<[u8]>` to `alloc_zeroed` with explicit 16-byte alignment and matching `dealloc` on drop, eliminating provenance ambiguity under miri's stacked-borrows model. Drop impl audit comment added. Non-global-allocator note added to the Arena type-level doc. CHANGELOG.md added in Keep a Changelog format. New `mixed_allocator` example demonstrates the arena coexisting with the global allocator. miri runs clean for 21 of 22 tests. cargo publish --dry-run succeeds. |
| 2026-05-08 | keleusma-arena pre-publication final pass. Tree borrows verified clean. docs.rs metadata block added so feature-gated APIs render and doc_cfg activates. Doctest added on Arena::with_capacity to catch documentation drift. CI extended with miri job covering both stacked and tree borrows, MSRV pin job at Rust 1.85 covering default and no-default-features builds, and clippy upgraded to --workspace --all-targets to lint examples. |
| 2026-05-08 | keleusma-arena docs polish. Activated the docs.rs alloc feature badge by adding cfg_attr doc_cfg to Arena::with_capacity. Wired README into crate-level documentation via include_str so all README code blocks run as doctests under cargo test --doc. Rewrote Static-Buffer Use example to use core::ptr::addr_of_mut for edition 2024 compatibility and inline form. Existing structured reference content kept as the API Reference subsection following the README intro. Six doctests pass total (five from the README, one on Arena::with_capacity). |
| 2026-05-08 | keleusma-arena v0.1.0 published to crates.io. Crate listing live with description, keywords, categories, license, homepage, and repository fields. cargo publish packaged 12 files at 55.1 KiB total. Documentation rendering on docs.rs. Workspace dependency line in keleusma updated to consume keleusma-arena from the registry rather than through the workspace path. keleusma-arena remains a workspace member for arena-side development. |
| 2026-05-08 | V0.1-M1 complete. Precompiled bytecode loading and trust-based verification skip. postcard wire format with KELE magic and 16-bit version. Module::to_bytes and Module::from_bytes accept any addressable byte slice including rodata. unsafe Vm::new_unchecked and unsafe Vm::load_bytes_unchecked skip WCET and WCMU bounds checks while preserving structural verification. R39 records the design. P10 records zero-copy execution as deferred path B. 286 keleusma unit tests, 7 new for precompiled bytecode. Phase advances to V0.1. |
| 2026-05-08 | V0.1-M1 hardened with CRC-32 algebraic self-inclusion. Hand-rolled bit-by-bit CRC-32 added as a four-byte little-endian trailer covering magic, version, and body. Verification runs the CRC once over the entire slice including the trailer and compares against the residue constant 0x2144DF1C in a single pass without zero-fill or position-aware special casing. LoadError gains a BadChecksum variant. R39 and EXECUTION_MODEL describe the residue property. 288 keleusma unit tests including new tests for the residue against crc32("123456789") = 0xCBF43926 and bad-checksum rejection. |
| 2026-05-08 | V0.1-M1 wire format extended with length, word size, address size, and golden-bytes test. BYTECODE_VERSION bumped to 2. Header grew to twelve bytes adding a 32-bit total length field, 8-bit word_bits, and 8-bit addr_bits. Recorded length is authoritative so trailing bytes after the recorded length are ignored, supporting embedded use cases. LoadError gains WordSizeMismatch and AddressSizeMismatch variants. The golden-bytes test pins the exact 37-byte serialization of `fn main() -> i64 { 1 }` to guard against wire format drift and endian regressions. 294 keleusma unit tests, 339 workspace total. |
| 2026-05-08 | V0.1-M1 word and address sizes encoded as base-2 exponents with relaxed acceptance and integer masking. BYTECODE_VERSION bumped to 3. Powers-of-two encoding covers 1 through 256 bits at exponents zero through eight, sufficient for the stated 6502-to-ARM64 target range. Runtime accepts bytecode whose exponent is at most the runtime's. VM applies sign-extending truncation in arithmetic ops to make narrower-on-wider semantics match the bytecode's declared overflow points. New tests confirm narrower bytecode acceptance and 32-bit overflow semantics under masking. Foundation for B10 target portability. 296 keleusma unit tests, 341 workspace total. |
| 2026-05-09 | V0.1-M2 Phase 1 of P10. Body format switched from postcard to rkyv 0.8. BYTECODE_VERSION bumped to 4. serde and postcard dependencies removed. Recursive Value type uses rkyv omit_bounds on self-referential fields with explicit serialize, deserialize, and bytecheck bounds. Header padded to 16 bytes with four reserved bytes for body alignment. Body alignment ensured during deserialization through rkyv AlignedVec copy. Phase 2 (true zero-copy execution against a borrowed buffer with Vm lifetime parameter) deferred to V0.1-M2-T2. 296 keleusma unit tests, 341 workspace total. |
| 2026-05-09 | V0.1-M2 Phase 2 step 1 of P10. Module::access_bytes returns a borrowed ArchivedModule from an aligned slice via rkyv::access without copying the body. Module::view_bytes uses access_bytes and rkyv::deserialize to skip the AlignedVec copy that from_bytes performs. Vm::view_bytes and unsafe Vm::view_bytes_unchecked compose validation with the safe and unchecked Vm constructors. Three new tests cover the aligned and unaligned paths and direct archived inspection. The execution loop continues to operate on the deserialized owned Module. Step 2 (execution against ArchivedModule) remains open. 299 keleusma unit tests, 344 workspace total. |
| 2026-05-09 | V0.1-M2 Phase 2 step 2 foundations of P10. Op derives Copy. bytecode::op_from_archived materializes an owned Op from an archived form for all 48 variants. bytecode::value_from_archived materializes an owned Value recursively for all 11 variants. Round-trip tests confirm the converters preserve identity through the archive cycle. The execution loop refactor that consumes these converters is recorded as V0.1-M2-T4 and remains open as multi-session work. 301 keleusma unit tests, 346 workspace total. |
| 2026-05-09 | V0.1-M2-T4 P10 Phase 2 step 2 complete. Vm gained lifetime parameter Vm<'a>. Internal storage shifted to BytecodeStore<'a> enum carrying Owned AlignedVec or Borrowed slice. Execution loop rewritten to read via archived() helper and per-access converters. Cold-path methods deserialize to owned Module on call. unsafe Vm::view_bytes_zero_copy is the true zero-copy constructor. End-to-end test confirms a program runs against a borrowed AlignedVec slice with no owned Module materialized. P10 resolved across all phases. 302 keleusma unit tests, 347 workspace total. |
| 2026-05-09 | V0.1-M3-T1 P1 type checker. New src/typecheck.rs with a two-pass design that collects type and function signatures then walks function bodies. Type enum with I64, F64, Bool, Unit, Str, Tuple, Array, Option, Struct, Enum, Opaque, and Unknown variants. Coverage includes signatures, let bindings, arithmetic, comparisons, field access, struct construction, casts, identifier resolution, if branches, for loops, enum variants, logical operators, and indexing. 13 unit tests pass. The pass is standalone, callable via typecheck::check(program). Integration into compile blocked on a parser refactor for the unit literal representation. 315 keleusma unit tests, 360 workspace total. |
| 2026-05-09 | V0.1-M3-T9 type checker gaps closed. Multiheaded function parameter types tracked through compile_pattern_bind_typed. Native function call types now distinguished from undefined names (latter rejected). Pattern type checking validates structural shape against scrutinee. Match exhaustiveness checked for enum, bool, unit, and other types. 15 new tests covering the four areas. 344 keleusma unit tests, 389 workspace total. |
| 2026-05-09 | V0.1-M3-T10 P7 follow-on partial. Host-owned arena and KString boundary type. Vm migrated to a shared reference to a host-pre-allocated Arena under a new 'arena lifetime parameter. Arena gained a u64 epoch counter that advances on reset, EpochSaturated for the saturating refusal, and an unsafe force_reset_epoch recovery path for very long-lived deployments. New ArenaHandle<T: ?Sized> generic wrapper and KString = ArenaHandle<str> alias deliver lifetime-free epoch-tagged stale-pointer detection through the get(&arena) accessor. Vm::reset_arena_internal routes through Arena::reset_unchecked under a documented safety contract. The constructor surface collapsed to Vm::new(module, arena) and Vm::new_unchecked(module, arena). 16 new tests across the workspace cover the lifecycle, saturation, recovery, and copy-out boundary patterns. P7 items 5 and 6 resolved. Items 7 (operand stack arena migration) and 8 (Value::KStr after the ArchivedValue split) deferred. 405 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T11 P7 fully resolved. Operand stack migrated to allocator_api2::vec::Vec<T, BottomHandle<'arena>>. New Arena::reset_top_unchecked clears the top region and advances the epoch while preserving bottom-region collections. Vm distinguishes between top-only reset for Op::Reset and full reset for error recovery and hot swap; the latter drops and recreates the arena-backed stacks before clearing both ends. ConstValue introduced as the rkyv-archived compile-time-constant type. Value enum split off rkyv derive and gained KStr(KString) variant. Value::from_const_archived lifts archived constants. Value::as_str_with_arena resolves KStr via arena context. Value::contains_dynstr extended to include KStr for the cross-yield prohibition. Compiler emits ConstValue at the boundary through ConstValue::try_from_value. Seven new boundary tests cover Value::KStr surface. Bounded memory now holds end to end for the operand stack and KString-allocated dynamic strings. The remaining unbounded path is Value::DynStr from natives that lack arena context. 412 tests pass workspace-wide. R40 records the implementation. |
| 2026-05-09 | V0.1-M3-T12 Native ABI arena context, float width log2, B9 documentation, BACKLOG hygiene. New NativeCtx<'a> public type carries a borrow of the host-owned arena and is constructed at every Op::CallNative dispatch. New Vm::register_native_with_ctx and Vm::register_native_with_ctx_closure register natives that opt into the context. The marshall layer's IntoNativeFn and IntoFallibleNativeFn traits accept the context internally and ignore it for marshalled functions. New register_utility_natives_with_ctx registers to_string and length through the ctx-aware ABI; to_string returns Value::KStr for bounded-memory output. Wire format gained float_bits_log2 byte at header offset 12; BYTECODE_VERSION bumped to 5; new LoadError::FloatSizeMismatch variant. Future portability for narrower floats (f32) tracked under B10 will use this field. B9 marked resolved structurally because Value::from_const_archived copies StaticStr to owned String at lift time. B11 added to BACKLOG for per-op decode optimization. Three stale V0.0-M6 rows marked complete. 415 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T13 B1 Hindley-Milner foundation. New Type::Var(u32) variant for inference variables. Subst type maps type variables to types. unify implements Robinson's algorithm with the occurs check. VarGen allocates fresh type variables. Type::occurs and Type::apply support the substitution machinery. UnifyError carries structural failure detail (Mismatch, OccursCheck, ArrayLengthMismatch, TupleArityMismatch). Ctx gains vargen and subst fields. types_compatible migrated from structural-equality check to unify-based constraint recording. Type::Unknown returns at every former narrow-inference site replaced with ctx.fresh(); existing Type::Unknown match arms extended to also accept Type::Var to preserve the permissive transitional behavior. 18 new unit tests cover the foundation primitives. The full substitution-application pass at end of function check and removal of the Type::Unknown sentinel are deferred future work for after B2 (generic types) lands. 433 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T14 B1 deferred and B2 generic functions. Substitution apply at end of check_function resolves locals to inferred types; per-function variables roll back via snapshot to keep typing local. Surface syntax fn name<T, U>(args) -> ret { body } parsed and represented in the AST through a new TypeParam type and FunctionDef::type_params field. Type checker records abstract Type::Var per type parameter in FnSig::type_param_vars. Call sites use instantiate_sig to substitute abstract variables with fresh per-call variables before unifying with actual arguments. Two distinct call sites instantiate independently. Keleusma's runtime-tagged Value enum dispatches polymorphically at runtime, so generic functions work end to end without explicit monomorphization. Five new parser tests, four new typecheck tests, one new example (generic_identity). Compile-time monomorphization for performance is now a future optimization rather than a correctness requirement. 441 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T15 B2.2 Generic struct and enum declarations. StructDef and EnumDef carry type_params: Vec<TypeParam>. Parser parses optional <T, U> after the struct/enum name through a shared parse_optional_type_params helper. Type::Struct and Type::Enum extended to carry Vec<Type> of per-instance type arguments. Type::occurs, Type::apply, Type::display, and unify updated to recurse through the args. Pass 1b allocates abstract Type::Var per declared type parameter and resolves field/variant types through from_expr_with_params. New struct_type_param_vars and enum_type_param_vars maps in Ctx record the abstract variables. New build_instance_subst helper. StructInit and EnumVariant construction sites instantiate fresh per-call variables and apply the substitution to declared field/payload types before unifying with provided values. Field access on Type::Struct(name, args) builds a per-instance substitution from the abstract variables to the per-instance args and applies it to the declared field type. types_compatible's Type::Var early-return removed so unification properly records constraints across distinct generic instantiations. Five new typecheck tests cover generic struct construction, generic enum construction, field access through instantiation, multi-parameter structs, and same-type-parameter constraint enforcement. New example generic_struct demonstrates end-to-end execution. 446 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T16 B2.2 follow-on (TypeExpr::Named generic args) and B2.3 trait declarations and bounds. TypeExpr::Named extended to carry Vec<TypeExpr> of generic arguments so types like `Cell<T>` parse and resolve in field/parameter/return positions. New trait and impl keywords. Parser parses `trait Name<T> { fn method(args) -> ret; }` and `impl Trait for Type { method definitions }`. New TraitDef, ImplBlock, and TraitMethodSig AST types. TypeParam carries bounds: Vec<String> populated from `<T: Trait1 + Trait2>` syntax. FnSig records type_param_bounds parallel to type_param_vars. Pass 1d in check() registers trait declarations and impls keyed on (TraitName, head TypeName). instantiate_sig now returns the per-call fresh variables alongside the instantiated parameter and return types. Call sites validate trait bounds by resolving each bounded fresh variable through the active substitution and checking the resulting head type against the trait impls registry; types lacking an impl are rejected with "type X does not implement trait Y" errors. Two new typecheck tests for B2.2 follow-on (pattern match on generic enum, generic struct field type) and six new tests for B2.3 (trait declaration accepted, satisfied bound, unsatisfied bound rejection, unbounded admits any, multiple bounds, missing one of multiple bounds). New example generic_match. Trait method dispatch and impl-method-vs-trait validation deferred. 454 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T17 B2.3 deferred (impl method registration and signature validation) and B2.4 monomorphization design. Each impl block's methods are registered in ctx.functions under the mangled name `Trait::TypeHead::method`. Compiler folds impl methods into the function group set under their mangled names so they emit as regular chunks. Pass 1e validates impl method signatures against the trait declaration: arity match, name match (presence in trait), unknown trait reference rejection. Three new typecheck tests cover impl_method_with_extra_method_rejected, impl_method_arity_mismatch_rejected, impl_for_unknown_trait_rejected. The B2.4 monomorphization design is recorded in BACKLOG with four phases: call-graph traversal, specialization generation, trait method resolution within specializations, and output. Implementation deferred to a focused future session because the cascade through compiler and runtime is substantial. Method call surface syntax (`x.method(args)`) folds into B2.4 because monomorphization-rewriting subsumes explicit dispatch. 457 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T18 B2.3 method call dispatch and B2.4 monomorphization MVP. New Expr::MethodCall variant. Parser distinguishes field access from method call by looking ahead for `(` after `expr.name`. Type checker resolves receiver type, takes its head, and looks up the registered impl method `Trait::Head::method` in ctx.functions. Compiler's MethodCall arm uses infer_expr_type plus type_expr_head to resolve the receiver's head, then searches the function map for any entry ending with `::Head::method`. New monomorphize.rs module with monomorphize() pass that runs between typecheck and compilation. The pass walks call sites of generic functions, infers concrete type arguments from literal arguments and identifiers with declared types, and generates specialized FunctionDef per (function, type_args) pair through type substitution in param/return/body. Specialization names use double-underscore mangling. After specialization, the compile pipeline re-runs typecheck so specialized bodies benefit from concrete-type method resolution. Generic functions with at least one specialization are dropped from the output. End-to-end example monomorphize_generic_method.rs demonstrates `fn use_doubler<T: Doubler>(x: T) -> i64 { x.double() }` resolving its method call after monomorphization. Three new typecheck tests cover method dispatch, method-not-found rejection, and the monomorphization round trip. 460 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T19 B2.4 follow-on (polymorphic recursion guard, prune unspecialized generics), B2.3 follow-on (full impl-vs-trait signature validation), and B3 closures parser/AST/typecheck. Monomorphization gains a SPECIALIZATION_LIMIT bound that prevents unbounded specialization in polymorphically-recursive call graphs. After specialization the pass drops every function with non-empty type_params, surfacing remaining generic call sites as compile errors rather than dead code. Pass 1e in typecheck now validates impl method parameter and return types against the trait declaration's structurally, not just arity. Two new typecheck tests cover param-type and return-type mismatch rejection. Closures gain surface syntax `|args| body` and `|args| -> ret { body }`. New Bar token for the bare `|`; bare `|` previously errored. New Expr::Closure variant. Parser handles closure literals in primary expression position. Type checker walks the body in a fresh parameter scope and returns a fresh type variable for the closure. Monomorphization recurses through closure bodies. Compiler rejects closure expressions with a clear deferred-runtime error. Three new parser tests cover nullary closures, single-param closures, and block-body closures. 465 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T20 B3 closure runtime end to end. New Value::Func(u16) runtime variant, Op::PushFunc(u16) and Op::CallIndirect(u8) instructions, and closure hoisting pass in the compile pipeline. Each Expr::Closure rewrites to Expr::Ident { name: __closure_<n> } and appends a synthetic FunctionDef. Compiler resolves Expr::Ident against function_map after locals to emit Op::PushFunc, and resolves Expr::Call where the name is a local to indirect dispatch through Op::CallIndirect. Type checker treats name-as-local Call as an indirect call returning a fresh type. BYTECODE_VERSION bumped to 6. Two new typecheck tests for closure round trips. New example closure_basic demonstrates `let f = |x: i64| x + 1; f(41)` returning 42 end to end. Environment capture deferred to a follow-on session. 467 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T21 B3 closure environment capture. Value::Func variant extended to carry env: Vec<Value> for captured values. New Op::MakeClosure(chunk_idx, n_captures) builds Value::Func with non-empty env. Op::CallIndirect updated to push env values as implicit arguments before explicit ones at invocation. New Expr::ClosureRef { name, captures, span } AST variant produced by the hoist pass to carry the captured names. Hoist pass collects free variables in closure bodies (identifiers not bound by the closure's params), filters natives and qualified paths, prepends captured names as synthetic-function parameters, and emits ClosureRef. Compiler ClosureRef arm emits GetLocal for each capture (or PushFunc for function names) followed by MakeClosure (or PushFunc when no captures). Type checker accepts ClosureRef as a fresh type. New example closure_capture demonstrates `let n = 10; |x| x + n` returning x+n end to end. New typecheck test closure_captures_outer_local. 469 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T22 B2.4 inference reach and B3 first-class closures as args. Monomorphize::infer_arg_type extended to resolve function-call returns through a fn_returns map, tuple and array literals through recursive inference, cast expressions through their target type, enum variants by enum name, and if/match expressions by their first branch. Pruning policy relaxed: generic functions with no specializations are retained since they continue to execute correctly through runtime tag dispatch on Value tags, supporting first-class closure arguments where concrete types cannot be inferred. Two new typecheck tests: monomorphize_inference_through_function_call and closure_passed_as_argument. New example closure_as_arg demonstrates `fn apply<F>(f: F, x: i64) -> i64 { f(x) }` accepting a closure and invoking it through indirect dispatch. 470 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T23 B2.4 generic struct specialization. New specialize_structs pass walks the program for Expr::StructInit whose target struct has type parameters. For each, the pass infers the type arguments by matching declared field types against provided field values' types and emits a specialized StructDef with field types substituted. The StructInit's name rewrites to the mangled form (e.g. Cell__i64). Subsequent compilation sees the specialized struct as a regular non-generic struct, enabling compile-time field-type inference for method dispatch on field-typed receivers. New helper functions mangle_struct, specialize_struct, rewrite_struct_inits_block/stmt/expr. New typecheck test monomorphize_struct_field_method_dispatch. New example struct_method_dispatch demonstrates `c.value.double()` where `c: Cell<i64>` resolving end to end. 471 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T24 B2.4 generic enum specialization, polymorphic recursion cycle detection refinement, and B3 capture-by-reference disposition. New specialize_enums pass mirrors specialize_structs for Expr::EnumVariant whose target enum has type parameters. Payload values' inferred types determine the type arguments. The pass emits a specialized EnumDef with variant payload types substituted and rewrites the EnumVariant's enum_name to the mangled form. Polymorphic recursion guard refined: in addition to the global SPECIALIZATION_LIMIT, a PER_FUNCTION_LIMIT caps the number of specializations any single generic function may produce. The fixed-point loop tracks per-function counts and exits early when any single function exceeds its bound, the structural signature of polymorphic recursion. Capture-by-reference closed as not applicable: Keleusma's let bindings are immutable by surface design, so a captured local cannot diverge from its captured snapshot regardless of capture strategy. The data segment is the only mutable mechanism and is accessed through data.field syntax independent of closure capture. New typecheck test monomorphize_enum_specialization_round_trip. 472 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T25 B2.4 inference reach (FieldAccess), B2 doc cleanup, and B3 nested-closure transitive capture. Monomorphize::infer_arg_type extended with a new optional struct table parameter. The FieldAccess arm resolves the object's nominal type, looks up the struct's declared field type, and applies per-instance type-argument substitution when concrete type args are available. Abstract field types whose declared type is one of the struct's type parameters are guarded against erroneous propagation as concrete type arguments. Struct table built once at the top of monomorphize and threaded through rewrite_block, rewrite_stmt, rewrite_expr, and rewrite_iterable. Compiler::collect_free_in_expr's ClosureRef arm rewritten to propagate inner closure captures to the enclosing scope's free-variable set. Previously, a `ClosureRef` was treated as a leaf, so an inner closure could not transitively capture from the outer-function scope through an outer closure. Now an inner closure's capture list contributes to the outer's capture list (for names not bound in the outer's parameters), and the outer closure's hoisted chunk receives the name as an additional implicit parameter. B2 backlog entry updated to remove the stale "method call surface syntax" item, since `Expr::MethodCall` is implemented and exercised by the monomorphization pipeline. New typecheck tests monomorphize_inference_through_field_access, closure_nested_inside_closure_typechecks, and closure_nested_capturing_outer_local_typechecks. New example closure_nested demonstrates `let outer = |x| { let inner = |y| base + x + y; inner(3) }; outer(7)` returning 110 end to end. 475 tests pass workspace-wide. |
| 2026-05-10 | V0.1-M3-T47 Documentation pass for the SDL3 example. The top-level `README.md` Examples section now mentions hot code swap and documents the `s` and Enter keyboard controls. `docs/README.md` Quick Reference adds rows for the script library, embedding examples, and the piano_roll demo. `docs/guide/EMBEDDING.md` Cross-References gains a piano_roll entry. The Hot Code Swapping subsection in EMBEDDING.md had two correctness bugs fixed (missing `initial_data` argument and `resume`-vs-`call` after replace_module); both were latent in the docs and surfaced when the example exercised the path. `docs/guide/GETTING_STARTED.md` Next Steps and `docs/guide/README.md` Companion Material both gained piano_roll entries. 519 tests pass; doc-only change. |
| 2026-05-10 | V0.1-M3-T46 Hot code swap in the SDL3 example. Two songs precompiled at startup; pressing `s` triggers `Vm::replace_module` at the next reset boundary; pressing Enter quits. Audio rendering continues across the swap. Song 2 (`piano_roll_2.kel`) uses an eighth-note arpeggiated melody, quarter-note alternating harmony, and a staccato bass; channel-to-role routing is reordered relative to song 1 so the swap produces a timbral as well as a melodic change on the fixed host instrument table. Workspace remains SDL3-free; 519 tests pass; clippy clean. |
| 2026-05-10 | V0.1-M3-T45 Refactor SDL3 example to gated Cargo example. The `keleusma-piano-roll` workspace member was refactored into a feature-gated Cargo example. The example now lives at `examples/piano_roll.rs` plus `examples/piano_roll.kel` with SDL3 declared as an optional dependency on the parent `keleusma` crate behind the `sdl3-example` feature. Standard workspace builds and tests no longer pull in SDL3. Run with `cargo run --release --example piano_roll --features sdl3-example`. The `keleusma-piano-roll/` workspace member directory was removed. README.md updated. Architecture and rationale notes from the removed directory's README folded into the file-level doc comment on `piano_roll.rs`. 519 tests pass workspace-wide; clippy clean. |
| 2026-05-10 | V0.1-M3-T44 SDL3 audio example. New `keleusma-piano-roll` workspace member with single Rust file and single Keleusma file. Three voices (square melody, triangle bass, square harmony) play a four-bar I-vi-IV-V progression in C major auto-looping at 120 BPM with 16th-note tick resolution. SDL3 audio thread synthesizes samples; Keleusma main thread sequences notes per tick; thread-safe handoff via `Arc<Mutex<[Voice; 3]>>`. Per-channel instrument params (waveform, volume) live as Rust constants; song notes live in `song.kel` as hard-coded arrays of `(Pitch, octave, duration_in_16ths)` tuples. Press Enter to quit. SDL3 builds from source via `build-from-source-static` (CMake required). Demonstrates the embedding pattern most production scripting hosts need: real-time deadline workload, shared state across threads, multi-voice control flow through the data segment. Three documentation bug fixes surfaced during authoring (data-block syntax, use-declaration position, if-else statement-position semicolons) corrected in WHY_REJECTED.md and the script. Workspace remains 519 tests pass; clippy clean. |
| 2026-05-10 | V0.1-M3-T43 Onboarding documentation. New `docs/guide/` section with `GETTING_STARTED.md`, `EMBEDDING.md`, and `WHY_REJECTED.md`. Eight standalone `.kel` scripts under `examples/scripts/` covering arithmetic, structs, enums, for-in, pipelines, multiheaded dispatch, f-strings, and method dispatch. Each script verified to run through `keleusma run` with documented expected output. Closes the onboarding gap relative to comparable scripting languages such as Rhai's Book. The guide acknowledges Keleusma's distinctive constraints, including immutable locals requiring data-segment or host-fold rewrites for accumulation, and threads the conservative-verification stance through `WHY_REJECTED.md`. Top-level `README.md` and `docs/README.md` updated to surface the guide. 519 tests pass workspace-wide; format and clippy clean. Documentation-and-examples-only change. |
| 2026-05-10 | V0.1-M3-T42 Standalone CLI. New `keleusma-cli` workspace member providing the `keleusma` binary modeled after Rhai's CLI ergonomics. Subcommands `run`, `compile`, and `repl`; shorthand `keleusma file.kel`. Runner pre-registers utility and math natives. Compiler writes the standard rkyv-framed wire format; default output path is `<input>.kel.bin`. REPL accumulates a declaration prefix across evaluations, distinguishes declarations from expressions, and tries return types i64, f64, bool, String, () in order. REPL commands `:help`, `:quit`, `:reset`, `:show`. Limitations documented in README: yield/resume not driven interactively, data-segment values not persisted across REPL evaluations, fixed return-type inference list, no cross-target compilation. Installation: `cargo install --path . --bin keleusma`. 0BSD license. Smoke-tested: hello.kel runs to 42, compile produces 220-byte bytecode, REPL defines and invokes functions correctly. 519 tests pass workspace-wide; format and clippy clean. |
| 2026-05-10 | V0.1-M3-T41 Cost-model calibration tool. New keleusma-bench workspace member with library and CLI binary. Measures pipelined cycles per opcode on the host CPU and emits a Rust source fragment defining `measured_op_cycles` and `MEASURED_COST_MODEL` for the host to include into its build. Architecture extensibility through the new `CycleCounter` trait with built-in implementations for x86_64 (RDTSC), AArch64 (CNTVCT_EL0), and a portable `Instant`-based fallback. Adding a new architecture requires implementing `CycleCounter` and adding a cfg arm to `default_counter`. Opcode coverage through the `OPCODE_SPECS` table; adding a new opcode requires appending an entry with build/cleanup pattern and required constants. Benchmark engine is architecture-independent: builds a Func chunk inlining the pattern PATTERN_REPETITIONS times (default 100,000), runs WARMUP_PASSES warmups, runs MEASUREMENT_PASSES measurement passes (default 16), takes the minimum across passes as the pipelined-cycle approximation. Internal arithmetic is f64 to preserve precision when counter resolution is coarse relative to per-pattern cost. Per-op values clamp to minimum 1 so the cost model never reports zero cycles. Source emitter generates a category-aggregated `match` over Op variants matching the structure of `nominal_op_cycles`. CLI binary writes generated source to stdout or a file via `--output`. README explains methodology and limitations including cache effects, branch prediction, and frequency scaling. Benchmark verified to run on AArch64 host producing ordering-correct relative measurements (data movement < arithmetic < composite construction). 519 tests pass workspace-wide (445 keleusma unit + 17 marshall + 17 kstring_boundary + 28 keleusma-arena + 6 doctests + 6 keleusma-bench + several other test crates). Format and clippy clean. |
| 2026-05-09 | V0.1-M3-T40 Document WCET in pipelined cycles. The unit terminology shifts from "nominal cycles" to "pipelined cycles" with explicit definition. A pipelined cycle is a CPU cycle in which the host's pipeline operates at steady-state throughput, assuming warm caches, correctly predicted branches, and no memory-bus contention. The metric maps to what CPU optimization tables (Agner Fog, Intel Optimization Reference Manual) call throughput or reciprocal throughput per instruction. LANGUAGE_DESIGN WCET section restructured with five subsections: Units (defines pipelined cycles and bytes); What the language guarantees (definitive pipelined-cycle bound); Caveats for actual cycles (gap from stalls); Caveats for wall-clock time (clock period and frequency scaling); Bounded order-of-magnitude WCET (the calibration-factor approach for practical applications). Industry terminology used: calibration factor / dilation factor for the platform-specific scalar that converts pipelined cycles to wall-clock WCET; time-predictable platform for the JOP-class architectures where calibration factor approaches unity. The pipelined-cycle bound is order-of-magnitude correct relative to actual wall-clock WCET on a specific deployment platform; for many practical applications this is sufficient when multiplied by a deployment-validated calibration factor. Source-level docs in bytecode.rs CostModel, NOMINAL_COST_MODEL, nominal_op_cycles, and Op::cost updated to use pipelined-cycle terminology consistently. README WCMU and WCET feature bullet expanded to mention pipelined cycles, calibration factor, and the cross-reference. 513 tests pass workspace-wide. Documentation-only change. |
| 2026-05-09 | V0.1-M3-T39 Wired WCET cycle and WCMU byte cost-model tables. New `bytecode::CostModel` struct with `value_slot_bytes` and `op_cycles` fields. New `NOMINAL_COST_MODEL` constant exporting the bundled unmeasured defaults. New `nominal_op_cycles` free function holding the cycle table that `Op::cost` now delegates to. New `CostModel` methods: `cycles`, `slots_to_bytes`, `heap_alloc_bytes`. The pre-existing `Op::cost` and `Op::heap_alloc` methods refactored to delegate to `NOMINAL_COST_MODEL`. Unit conventions documented at the source level: WCMU is bytes (target-independent in principle, runtime-dependent through `value_slot_bytes`); WCET is **nominal cycles** (unmeasured estimates suitable for relative ordering, not validated cycles for any specific host CPU). New `verify::verify_resource_bounds_with_cost_model` public entry point accepts a host-supplied cost model. The current implementation delegates to the nominal-model path; full threading of the cost model through `module_wcmu` and the per-chunk WCMU computation is recorded as a tracked refinement under the B10 cost-table follow-on. Five new unit tests in bytecode.rs cost_model_tests module: nominal_cost_model_value_slot_bytes_matches_constant, nominal_cost_model_cycles_match_op_cost_method, cost_model_slots_to_bytes_uses_slot_size, cost_model_heap_alloc_bytes_scales_with_slot_size, custom_cost_model_returns_custom_cycles. The custom-cost-model test demonstrates that a host-supplied `op_cycles` function flows through `CostModel::cycles`. LANGUAGE_DESIGN WCET Analysis section restructured to document units explicitly, distinguish nominal cycles from measured cycles, and explain the cost-model construction surface. lib.rs re-exports `CostModel`, `NOMINAL_COST_MODEL`, `VALUE_SLOT_SIZE_BYTES`, and `nominal_op_cycles`. 513 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T38 Release-readiness pass. Four items: (1) CLAUDE.md project file updated to reflect V0.1-M3 status, the conservative-verification stance, and the new modules (visitor, target, monomorphize, typecheck). (2) Top-level README.md rewritten to lead with the WCET and WCMU value proposition, accurately describe the API surface including arena and target descriptors, fix the stale Vm::new signature in the Quick Start, correct the dependency list, fix the license to 0BSD, and add cross-references to the conservative-verification stance. (3) Cargo.toml metadata extended with homepage, repository, documentation, readme, rust-version, and docs.rs metadata block; description updated to lead with the WCET value prop; rust-version bumped to 1.87 to match the actual code (was incorrectly 1.85 with code using is_multiple_of). (4) Compile-time WCET defense-in-depth added in compile_with_target. Two checks fire at compile time: structural verification (verify::verify) and unbounded-construct rejection (Op::CallIndirect, Op::MakeRecursiveClosure). Full WCMU computation including loop iteration bound extraction remains deferred to Vm::new because it requires the host's arena capacity and because some Func chunks have parameter-dependent loops the present analysis cannot extract. Two recursive-closure tests inverted to expect compile-time rejection. 508 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T37 Document conservative-verification stance. New section in LANGUAGE_DESIGN.md after Five Guarantees titled "Conservative Verification". Explains the property that the surface admits descriptions of programs the verifier rejects. Two categories formalized: first-category provably unbounded constructs that no future analysis would admit, and second-category bounded but not yet proven constructs that future analysis could admit without surface changes. Describes implications for hosts (script design within current verifier capability), tooling (highlight rejected constructs), and language evolution (admitted set grows as analysis matures). Worked examples cover Op::CallIndirect (second category), Op::MakeRecursiveClosure (first category), and apply(apply, x) (first category). EXECUTION_MODEL Indirect Dispatch and Recursion subsection cross-references the new stance. BACKLOG B3 entry references the stance to explain why the closure feature exists despite verifier rejection. LANGUAGE_DESIGN cross-references list extended. 508 tests pass workspace-wide. No code changes; documentation-only addition. |
| 2026-05-09 | V0.1-M3-T36 Plug the CallIndirect WCET hole. The language's load-bearing guarantee is definitive WCET and WCMU. The safe verifier (`verify::module_wcmu`) now rejects any module containing `Op::CallIndirect` in addition to the existing rejection of `Op::MakeRecursiveClosure`. The construction ops `Op::PushFunc` and `Op::MakeClosure` remain admissible because they produce values that can be yielded or stored without invocation; only dispatch through `Op::CallIndirect` is the load-bearing rejection. The valid form of unbounded execution is the top-level `loop` block, enforced by the structural verifier's productivity rule. Five closure examples removed: closure_basic, closure_capture, closure_as_arg, closure_nested, closure_recursive. The first four required `Vm::new` to admit them, which the strictified verifier no longer does. The fifth used `Vm::new_unchecked` to bypass the rejection, which the user has reframed as intentional misuse rather than a supported escape hatch. Two new verifier tests: `verify_resource_bounds_rejects_call_indirect` covers the rejection path; `verify_resource_bounds_admits_push_func_without_call_indirect` confirms that pushing a function value without invoking it remains admissible. EXECUTION_MODEL Indirect Dispatch and Recursion subsection rewritten to reflect the strict rejection rather than the prior "known approximation" language. BACKLOG B3 entry retitled "Implemented; not WCET-safe" and updated to clarify that closures are supported in the language pipeline but not advertised as part of the WCET-safe surface. LANGUAGE_DESIGN Scope Inclusions updated similarly. 508 tests pass workspace-wide. Format and clippy clean. |
| 2026-05-09 | V0.1-M3-T35 Visitor trait refactor for AST walkers. New `src/visitor.rs` module defines `MutVisitor` and `Visitor` traits with default-implemented `walk_block`, `walk_stmt`, `walk_expr`, `walk_iterable` methods that handle structural recursion. Each pass implements the trait via a state struct and overrides only the `visit_*` hooks it needs. Five passes migrated: (1) `target::TargetChecker` (immutable, replaces `check_block_against_target`/`_stmt`/`_expr`); (2) `compiler::FreeVarCollector` (immutable, replaces `collect_free_in_block`/`_stmt`/`_expr`); (3) `compiler::ClosureHoister` (mutable, replaces `hoist_in_block`/`_stmt`/`_expr` and the auxiliary `hoist_let_bound_closure`); (4) `monomorphize::EnumSpecializer` (mutable, replaces `rewrite_enum_variants_block`/`_stmt`/`_expr`); (5) `monomorphize::StructSpecializer` (mutable, replaces `rewrite_struct_inits_block`/`_stmt`/`_expr`); (6) `monomorphize::CallSpecializer` (mutable, replaces `rewrite_block`/`_stmt`/`_expr`/`_iterable`). The structural recursion that was previously copy-pasted across these six families now lives once in the trait's default `walk_*` methods. Each pass's `visit_*` overrides handle only the per-pass logic (state tracking through `Stmt::Let`, transformation at the relevant `Expr` variants). Net file size change: monomorphize.rs from 2156 to 1204 lines (-952, -44%), compiler.rs from 2805 to 2664 lines (-141), target.rs from 538 to 453 lines (-85), new visitor.rs at 305 lines. Total reduction across the four files: 873 lines. 506 tests pass workspace-wide. Format and clippy clean. |
| 2026-05-09 | V0.1-M3-T34 Code deduplication pass. Three concrete consolidations: (1) Span gained `derive(Default)` and the four-field literal `Span { start: 0, end: 0, line: 0, column: 0 }` constructions in target.rs replaced with `Span::default()`. (2) utility_natives.rs's eight near-duplicate native functions consolidated through four shared helpers: `check_arity` for the argument-count check, `read_string_arg` (Optional arena) for the string-extraction logic that previously had separate `_no_arena` and `_with_arena` variants, `read_i64_arg` for typed i64 extraction, `finalize_string_result` for the DynStr-or-KStr wrapping at the end of string-producing natives. The native_concat / native_concat_with_ctx pair now both delegate to `concat_impl(arena: Option<&Arena>, args)`. The native_slice / native_slice_with_ctx pair delegates to `slice_impl`. The to_string, to_string_with_ctx, length, length_with_ctx, and println functions also adopted `check_arity`. Net change: utility_natives.rs from 696 to 638 lines (-58, -8.3%). (3) vm.rs's `decode_all_ops` helper signature changed from `(&AlignedVec)` to `(&[u8])` so both the owned-bytecode constructor (`Vm::construct`) and the borrowed-bytecode constructor (`Vm::view_bytes_zero_copy`) route through the single source of truth. The borrowed path previously inlined the iteration; that inline loop is removed in favor of the helper call. Net change: vm.rs from 3748 to 3736 lines (-12). 506 tests pass workspace-wide. Format and clippy clean. |
| 2026-05-09 | V0.1-M3-T33 Documentation deduplication and streamlining pass. EXECUTION_MODEL.md gained an "Implementation Mapping" subsection that names the source-level types and methods realizing each ABI region (`.text` -> Module::chunks[i].ops + Vm::decoded_ops, `.rodata` -> Module::chunks[i].constants + struct_templates + native_names, `.data` -> Vm::data + Module::data_layout, `.bss` -> Vm::stack + Vm::frames + KString allocations). Lifetime invariants and memory bookkeeping documented. Module ownership mapping for `BytecodeStore` Owned vs Borrowed paths added. Stale postcard reference at line 129 corrected to rkyv with reference to R39. LANGUAGE_DESIGN.md streamlined: the duplicated memory-model table, the duplicated Two Temporal Domains list, and the duplicated Turing Completeness paragraph all replaced with brief references to EXECUTION_MODEL. Hot Code Swapping section trimmed to language-level summary referencing the canonical specification in EXECUTION_MODEL. Coroutine Model retained but extended with the resume-value error pattern (B7) reference. Scope Exclusions section renamed to "Scope Inclusions and Exclusions" and updated to reflect HM inference foundation, generics with traits and bounds, monomorphization, closures with all features, f-string interpolation, and string concatenation/slicing as implemented features. COMPILATION_PIPELINE.md pipeline diagram expanded to show the typecheck/monomorphize/typecheck/hoist/emit stages, compile_with_target signature added, recursion-detection note updated to point at verify::module_wcmu, Vm::new signature updated to take an arena. Documentation net change: LANGUAGE_DESIGN -16 lines, EXECUTION_MODEL +17 lines (due to new Implementation Mapping), COMPILATION_PIPELINE +17 lines (due to accuracy updates). Duplication is reduced; concrete information content is up. 506 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T32 B10 portability foundation. New `crate::target::Target` descriptor with three width fields (word, address, float as base-2 exponents) and two capability flags (has_floats, has_strings). Const presets: host (64-bit), wasm32 (32-bit word/addr, 64-bit float), embedded_32 (32-bit), embedded_16 (16-bit, no floats), embedded_8 (8-bit word with 16-bit address per 6502 class, no floats, no strings). New `compile_with_target(program, target)` API runs `validate_against_runtime` to reject targets exceeding runtime widths, runs `validate_program_for_target` to reject programs using unsupported features, and bakes the target widths into the resulting module's wire-format header. AST walkers `check_type_against_target`, `check_block_against_target`, `check_stmt_against_target`, and `check_expr_against_target` traverse function bodies, impl methods, and closures. The pre-existing `compile(program)` is now a thin wrapper over `compile_with_target(program, &Target::host())`. Nine new tests cover host preset, full-feature admission, float and string rejections, int-only programs on embedded_8, width propagation to the module, host widths matching runtime constants, and oversized-target rejection. New example target_aware_compile demonstrates compilation to host, embedded_32, embedded_16, and embedded_8 targets with the appropriate rejections. Cross-target codegen, target-specific Value representations, and target-defined primitive types (byte, bit, word, address) remain future work documented in BACKLOG. 506 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T31 B11 per-op decode optimization. The hot dispatch loop previously called `op_from_archived` for every instruction fetch, which performed a discriminant match plus a small payload copy. The VM now caches a per-chunk `Vec<Op>` populated at construction, indexed as `decoded_ops[chunk_idx][ip]`. The hot path reads through a direct slice index. New `decode_all_ops` helper walks the archived module's chunks and decodes every op into the cache. Both `Vm::construct` (owned bytecode) and `Vm::view_bytes_zero_copy` (borrowed bytecode) populate the cache. `Vm::replace_module` re-decodes for the new module. Cost: one heap allocation per chunk at construction proportional to total op count. Constants and string data continue to be read on demand from the archived form, so the zero-copy contract for those is preserved. 497 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T30 B7 error propagation through yield and B8 VM allocation model. B7 resolved as a resume-value pattern: the existing yield/resume cycle already supports any Value as the resumed input, so error propagation is implemented through the script's choice of resumed type. The script declares a Result-shaped enum (like `enum Reply { Ok(i64), Err }`) or `Option<T>`, the yield expression evaluates to that type, and the script pattern-matches on the variant. New `Vm::resume_err(error_value)` API is a documentation alias over `Vm::resume` that signals the host's intent to propagate an error rather than supply a successful input. Two new tests `resume_err_propagates_through_enum_reply` and `resume_err_passes_through_with_value_none` exercise both the success and error paths through the same enum-based dialogue pattern. New example `yield_error` demonstrates the pattern end to end with both `Ok(42)` and `Err` resumes. B8 closed as not-applicable: shared arena across multiple Vm instances is incompatible with the per-VM `verify_resource_bounds` contract, the per-arena KString epoch model, the per-VM Op::Reset semantics, the single-threaded arena ownership, and the per-VM cross-yield prohibition. The legitimate use cases (allocation overhead amortization across sequential scripts) are already supported by reusing one Arena across successive Vm lifecycles. 497 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T29 B5b string discipline extensions and B6 f-string interpolation. New utility natives `concat(s1, s2) -> String` and `slice(s, start, end) -> String` in both context-aware (returning `Value::KStr`) and non-context (returning `Value::DynStr`) variants. Helpers `string_view_no_arena`, `string_view_with_arena`, and `slice_chars` factor the value-to-string projection and code-point-based slicing. f-string interpolation lands as a lex-time desugaring. The lexer recognizes `f"..."` syntax, scans the body for `{...}` markers, recursively tokenizes interpolated expressions, and emits a left-associative chain of `concat` and `to_string` calls through a new `pending: VecDeque<Token>` buffer. Escapes `\{` and `\}` produce literal braces. Empty f-strings produce `""`; single-literal f-strings produce the bare literal; single-interpolation f-strings produce `to_string(<expr>)`; mixed f-strings produce nested concat calls. New typecheck and runtime tests: concat_two_static_strings, concat_static_with_dynamic, slice_basic, slice_full_range, slice_empty_range, concat_with_ctx_returns_kstr, slice_with_ctx_returns_kstr, fstring_no_interpolation, fstring_single_interp, fstring_mixed_interp, fstring_multiple_interps, fstring_escaped_braces. New example string_ops demonstrates the combined feature end to end. WCET note: string ops produce dynamic strings whose worst-case output is bounded by input lengths; hosts must declare heap bounds via `set_native_bounds` for real-time embedding. 495 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T28 B2.4 inference reach for MethodCall, UnaryOp, and BinOp. Probe identified three remaining gaps where a generic call argument's type was not inferred: a method-call return value (`use_doubler((21).double())`), a unary-negation (`use_doubler(-n)`), and an arithmetic binary operation (`use_doubler(n + 11)`). All three previously failed at compile because monomorphization could not specialize the generic call. Monomorphize::infer_arg_type extended with arms for Expr::MethodCall, Expr::UnaryOp, and Expr::BinOp. Method-call inference looks up the impl method's declared return type under a new `<head>::<method>` mangled key in fn_returns, which is now populated from program.impls at the top of monomorphize alongside top-level functions. UnaryOp::Neg preserves operand type and UnaryOp::Not returns Bool. Arithmetic BinOps (Add/Sub/Mul/Div/Mod) preserve left-operand type and comparison/logical BinOps (Eq/NotEq/Lt/Gt/LtEq/GtEq/And/Or) return Bool. New helper type_head_for_impl mirrors the compiler's existing type_expr_head convention so the mangled key is consistent. Three new tests: monomorphize_inference_through_method_call, monomorphize_inference_through_unary_op, monomorphize_inference_through_bin_op. 482 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T27 WCET safety for recursive closures. The recursive-closure mechanism added in V0.1-M3-T26 dispatches through `Op::CallIndirect`, which the WCMU analysis cannot follow. The pre-existing topological-sort recursion check only walks `Op::Call`, so recursive closures escape the cycle detection by construction. To preserve the soundness of `verify_resource_bounds`, `verify::module_wcmu` now rejects any module that contains `Op::MakeRecursiveClosure` with a clear error message. The safe constructors `Vm::new` and `Vm::load_bytes` therefore reject recursive-closure programs by default. The `examples/closure_recursive.rs` example was updated to use `Vm::new_unchecked` to opt out of the resource-bounds check explicitly. New test `verify_resource_bounds_rejects_recursive_closures` exercises the rejection path. EXECUTION_MODEL gained a new "Indirect Dispatch and Recursion" subsection that documents the WCET implications of indirect dispatch over closures, including the known approximation that the analysis does not follow indirect-dispatch targets and that programs constructing unbounded recursion through indirect dispatch over non-recursive closures (such as `apply(apply, x)`) are admissible despite being unbounded at runtime. 480 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T26 B2.4 inference reach (TupleIndex, ArrayIndex) and B3 recursive closures. Monomorphize::infer_arg_type extended to resolve TupleIndex by reading the indexed tuple element's type and ArrayIndex by reading the array's element type. Three monomorphize_inference_through_* tests refactored to use a new compile_src helper that exercises the full compile pipeline rather than just type-check, since the type checker accepts generic bodies that monomorphization may not be able to specialize. Added monomorphize_inference_through_tuple_index and monomorphize_inference_through_array_index tests. New BYTECODE_VERSION 7 with Op::MakeRecursiveClosure(chunk_idx, n_captures) and Value::Func gaining a `recursive: bool` field. Recursive closures via let-binding (`let f = |...| ... f(...) ...`) work end to end: hoist pass detects self-reference in Stmt::Let when the value is a closure and the binding name appears in the body's free vars, removes the binding name from regular captures, prepends remaining captures and inserts the binding name as the self parameter before explicit params, and sets recursive=true on the resulting ClosureRef. Compiler emits MakeRecursiveClosure for recursive ClosureRefs. VM CallIndirect handles recursive funcs by pushing the func itself between env values and explicit args. Type checker registers a fresh type variable for the binding before walking the closure value so the body's self-reference resolves rather than failing as undefined. Captures coexist with self-reference. New typecheck tests recursive_closure_typechecks and recursive_closure_with_capture_typechecks. New example closure_recursive computes `fact(5) == 120` end to end. Compiler also extended to allow ClosureRef captures that resolve to top-level/synthetic functions (PushFunc) when not present as locals. Golden bytes for the bytecode_golden test updated for version 7 and recomputed CRC. 479 tests pass workspace-wide. |
| 2026-05-09 | V0.1-M3-T7 P2 iteration variable type tracking. The compile_for path now records the element type of the for-in source on the iteration variable's local. Nested for-in over multi-dimensional arrays now resolves bounds at every level. Two new tests cover the verify path and end-to-end execution of a three-level nested for-in over a 2x2x2 i64 matrix. 327 keleusma unit tests, 372 workspace total. |
| 2026-05-09 | V0.1-M3-T6 P2 for-in over nested array indexing and match expression results. infer_expr_type and static_for_in_length both gained Expr::ArrayIndex and Expr::Match cases. element_type_of helper extracts the element type from TypeExpr::Array. Two new tests confirm verification of nested matrix indexing and match-expression-returning-array. P2 has no remaining deferred cases. 325 keleusma unit tests, 370 workspace total. |
| 2026-05-09 | V0.1-M3-T5 P2 local type tracking. The compiler now tracks local variable types via let annotations and a narrow inference set covering struct construction, function calls, identifiers, field access, array literals, and literal values. Parameters record declared types. The for-in iteration bound now resolves through identifier locals, so for-in over struct fields accessed through locals and over array parameters passes strict-mode WCMU verification. Two new tests cover these paths. P2 fully resolved for the practical cases; nested indexing and match results remain deferred future work. 323 keleusma unit tests, 368 workspace total. |
| 2026-05-09 | V0.1-M3-T4 P2 for-in over typed expressions. Compiler now infers static array length from typed expressions and emits Const(N) for the for-in end bound, allowing the strict-mode WCMU verifier to accept loops over function returns, data segment fields, and let-bound array literals in addition to the existing array-literal case. New TypeInfo struct in the compiler. Three new tests cover the typed paths. The remaining case is for-in over struct field access from a local, which requires local variable type tracking and is documented as follow-up. P2 resolved for typed cases. 321 keleusma unit tests, 366 workspace total. |
| 2026-05-09 | V0.1-M3-T3 P3 error recovery model. Vm::reset_after_error clears the operand stack, call frames, and arena while preserving the data segment. Hosts call this after a failed call or resume to recover into a callable state without losing accumulated state. Streams that fail mid-iteration can be retried with the data intact. Three unit tests cover the recovery cycle. P3 resolved. 318 keleusma unit tests, 363 workspace total. |
| 2026-05-09 | V0.1-M3-T2 P1 wired into compile pipeline. Added Literal::Unit AST variant. Parser produces Literal::Unit for `()` literal rather than Literal::Int(0). Compiler emits Op::PushUnit for Literal::Unit in expressions and patterns. Type checker recognizes Literal::Unit as Type::Unit. typecheck::check is invoked from compile and errors surface as CompileError. Five tests updated to declare the struct or enum types they reference, removing reliance on lax pre-check behavior. P1 is now resolved. 315 keleusma unit tests, 360 workspace total. |
