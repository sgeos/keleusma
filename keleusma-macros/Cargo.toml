[package]
name = "keleusma-macros"
version = "0.1.0"
edition = "2024"
description = "Procedural macros for the Keleusma scripting language"
license = "0BSD"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
